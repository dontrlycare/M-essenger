{
    "name": "m-essenger",
    "version": "1.0.0",
    "description": "M-essenger - Modern Messenger Application",
    "main": "index.html",
    "scripts": {
        "copy-assets": "rm -rf www && mkdir www && cp index.html www/ && cp -r desktop www/",
        "build": "npm run copy-assets"
    },
    "devDependencies": {
        "@capacitor/android": "^5.7.8",
        "@capacitor/cli": "^5.7.8",
        "@capacitor/core": "^5.7.8"
    }
}